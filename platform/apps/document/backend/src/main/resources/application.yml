server:
  port: 8085
spring:
  application:
    name: document-center
management:
  endpoints:
    web:
      exposure:
        include: health,info
document:
  library-path: ${DOCUMENT_LIBRARY_PATH:/documents}
